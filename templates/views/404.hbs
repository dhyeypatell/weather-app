<!DOCTYPE html>
<html lang="en">
  <head>
    {{>commonMeta}}
    <link rel="stylesheet" href="/styles/css/pages/404.css" />
  </head>
  <body>
    <div>
      {{>header}}
      <p>{{errorMessage}}</p>
    </div>
    {{>footer}}
  </body>
</html>
